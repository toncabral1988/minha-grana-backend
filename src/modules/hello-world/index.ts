import { TagOptions } from 'hapi-swagger'

const tag: TagOptions = {
    name: 'api',
    description: 'Api Minha Grana'
}

export default {
    name: 'Hello World',
    dirname: __dirname,
    prefix: '',
    tag 
}